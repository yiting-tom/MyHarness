## 1. 內容（先於畫面）

- [x] 1.1 `myharness/monitor/report.py`：異常與 caveat 的人話對照表。
      **這是這個 change 的內容，不是措辭** —— 每一條都要有測試
- [x] 1.2 來源鏈：重用 `DataFlow.provenance()`，不另外算一份
- [x] 1.3 「沒有被任何執行讀取的資料」要能單獨列出（`unused_input` 已經偵測了）

## 2. 畫面

- [x] 2.1 `myharness/monitor/report.html`：資料流是主體，來源鏈預設高亮
- [x] 2.2 淺色、可讀為主。跟 `inspect --html` 的終端機深色刻意分開 ——
      兩個受眾不該長得像同一個工具
- [x] 2.3 章節價目表，點開才給全文
- [x] 2.4 CJK 字型堆疊要明確，不能靠瀏覽器碰運氣

## 3. 接線

- [x] 3.1 `myharness report <job> [-o out.html]`；未給 `-o` 寫到 stdout
- [x] 3.2 重用 `build_delivery` —— 它已經是對外契約

## 4. 測試

- [x] 4.1 每一個 `AnomalyKind` 與每一個 caveat kind 都有人話，且沒有任何
      識別代碼漏到畫面上（規格：異常被翻成讀者能理解的說法）
- [x] 4.2 來源鏈：不在鏈上的執行被標成不在鏈上（規格：未貢獻的執行可被區分）
- [x] 4.3 乾淨的 job 明講「沒有問題」（規格：沒有問題時也要說）
- [x] 4.4 章節價目表在全文之前（規格：價目表先於全文）
- [x] 4.5 無外部資源、不改 job
- [x] 4.6 golden5 那次：報告來自沒有授權的派工，使用者要看得出來

## 5. 文件

- [x] 5.1 `myharness/monitor/README.md`：兩個受眾、兩個視圖，說清楚為什麼不合併

## 6. 實作時才發現的

- [x] 6.1 兩個視圖都把 payload 放進 `<script>`，也就是同一個 `</script>` 風險。
      抽成 `myharness/monitor/html.py`，解一次
- [x] 6.2 第一版把 `golden24/note/lanes/synth1/findings/synth_state` 這串 id
      直接印在使用者面前 —— **正是這個模組存在要防的那種洩漏**。
      `_where()` 改成用流向上已經在用的短標籤
- [x] 6.3 報告是 markdown 寫的，第一版把 `**` 原封不動印出來。改成轉 `<strong>`
      （先 esc 再轉，所以轉得出來的標籤只有這一種）
- [x] 6.4 標題已經是問題的第一句，底下的引文再印一次只是佔位置。改成只印其餘的
- [x] 6.5 每一段都在來源鏈上時，「只看來源／看全部」按了沒反應。整組隱藏 ——
      按了沒反應的控制項比沒有控制項更糟
